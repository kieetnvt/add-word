$(document).ready(function() {
  // Select2 JsPlugin
  $('.select2').select2();
  if (typeof $('#roled_list') !== undefined) {
    $roled_list = $('#roled_list').val().split(' ');
    $('.select2').select2('val', $roled_list);
  };
});
